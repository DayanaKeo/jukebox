import { create } from 'tailwind-react-native-classnames';

// On crée une instance simple sans surcharge de thème (twrnc ne supporte pas extend)
// lib/tw.ts
import tw from 'tailwind-react-native-classnames';
export default tw;
